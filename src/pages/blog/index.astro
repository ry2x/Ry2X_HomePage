---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getAllPosts, formatDate } from '../../lib/content';

const posts = await getAllPosts();
---

<BaseLayout
  title="Blog - My HomePage"
  description="Read about my experiences, tutorials, and thoughts on web development"
>
  <div class="mb-8">
    <h1 class="mb-4 text-3xl font-bold text-gray-900 dark:text-gray-100">Blog</h1>
    <p class="text-gray-600 dark:text-gray-400">
      Welcome to my blog where I share my experiences, tutorials, and thoughts
      on web development.
    </p>
  </div>

  {
    posts.length > 0 ? (
      <div class="space-y-8">
        {posts.map((post) => (
          <article class="border-b border-gray-200 pb-8 dark:border-gray-700">
            <div class="mb-4">
              <div class="mb-3 flex flex-col md:flex-row md:items-center md:justify-between">
                <h2 class="text-xl font-semibold text-gray-900 dark:text-gray-100">
                  <a
                    href={`/blog/${post.slug}`}
                    class="transition-colors hover:text-blue-600 dark:hover:text-blue-400"
                  >
                    {post.data.title}
                  </a>
                </h2>
                <time class="text-sm text-gray-500 dark:text-gray-400 md:text-base">
                  {formatDate(post.data.date)}
                </time>
              </div>

              <p class="mb-4 text-gray-600 dark:text-gray-400">{post.data.description}</p>

              {post.data.tags && post.data.tags.length > 0 && (
                <div class="mb-4 flex flex-wrap gap-2">
                  {post.data.tags.map((tag) => (
                    <span class="rounded-full bg-blue-100 px-2 py-1 text-xs text-blue-800 dark:bg-blue-900/50 dark:text-blue-300">
                      {tag}
                    </span>
                  ))}
                </div>
              )}

              <a
                href={`/blog/${post.slug}`}
                class="font-medium text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300"
              >
                Read more →
              </a>
            </div>
          </article>
        ))}
      </div>
    ) : (
      <div class="py-12 text-center">
        <div class="mb-4 text-gray-500 dark:text-gray-400">
          <p class="mb-2 text-lg">No blog posts yet!</p>
          <p>Check back later for new content.</p>
        </div>
        <div class="text-sm text-gray-400 dark:text-gray-500">
          <p>
            Add blog posts to:{' '}
            <code class="rounded bg-gray-100 px-2 py-1 dark:bg-gray-800">src/content/blog/</code>
          </p>
          <p>
            See content guidelines:{' '}
            <code class="rounded bg-gray-100 px-2 py-1 dark:bg-gray-800">
              src/content/README.md
            </code>
          </p>
        </div>
      </div>
    )
  }
</BaseLayout>
